name: LandLocker
version: V1.1.5
status: STABLE
message: |
  # LandLocker 1.1.5
  - Added in-game configurations to provide further support for realms. Therefore, file configuring is now removed.
  - Added slash commands cooldown.
  - Added default minimum size on land claims at resized.
  - Adjustment with default minimum size.